{* By design, this template will never display. The related page class
 * CRM_Eventbrite_Page_Webhook is a webhook listener which will
 * respond with a short bare string, i.e., no CMS or CiviCRM page chrome; as
 * such, it simply echoes that string and exits, so this template is never used.
 *}